version: '3'

services:
  scimgateway:
    build: ./scimgateway
    ports:
      - "8883:8883"
    restart: always
    environment:
      mongodb-new-connector_BASIC_0_USERNAME: wagner
      mongodb-new-connector_BASIC_0_PASSWORD: 1234

      DATABASE_URL: mongodb+srv://wagner:1234@cluster0.nbqcliu.mongodb.net/qscim?retryWrites=true&w=majority&appName=Cluster0
    volumes:
      - data:/home/node/app/data
   
  forms:
    depends_on:
      - scimgateway
    build: ./forms
    ports:
      - "8000:80"
    restart: always
  
     

volumes:
  data:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: /home/luciana/Documents/qscim-files/qap/data